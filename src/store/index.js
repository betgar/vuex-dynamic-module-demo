import Vue from 'vue';
import Vuex from 'vuex';
// import userStoreConfig from '../views/state/user.state';
Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    // userStore: userStoreConfig
  }
});
